#include"main.h"

/**
 * main - Entry point, check the code
 *
 * Description: Write a function that prints a-z alphabets
 *
 * Return: Always 0
 */

void print_alphabet(void);

{
	int (ch);
	for (ch = 'a'; ch <= 'z' ++ch)
		_putchar(str[ch]);
	_putchar('\n');

	return (0);
}
