#include"main.h"

/**
 * _islower - prints 1 0r 0 depending on input used
 * @c: first paramenter
 * Description: prints all lowercase letters
 * Return: Always 0 (success)
 */

int _islower(int c)
{

	return (c >= 'a' && c <= 'z');
}
