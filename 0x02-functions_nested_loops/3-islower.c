#include"main.h"

/**
 * main - islower function to check if characters are lowercase
 *
 * Description: @c checks input of function
 *
 * Return: 1 if 'c' is a lowercase or otherwise return 0
 *
 */

int _islower(int c)
{
	if (c >= 0 && c < z)
		return (1);
	else 
		return (0);

}
