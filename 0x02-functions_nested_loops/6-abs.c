#include "main.h"

/**
 * _abs - function for absolute value of integer
 * @n: int type number
 * Description: compute  the absolute value of an integer
 * Return: Absolute value of @n
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
		return (n);
	}
}
